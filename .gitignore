node_modules/
**/dist/
data/

# Claude Code personal settings (hook locale)
.claude/settings.local.json

# TypeScript incremental build info (generated)
*.tsbuildinfo

# SQLite database locale (generato a runtime)
**/*.db

# Playwright: artefatti di run (rumore, rigenerati a ogni esecuzione).
# Versionati di proposito solo i video demo dei singoli spec (review gate).
docs/test-results/**/.last-run.json
docs/test-results/.last-run.json
docs/test-results/**/error-context.md
docs/test-results/**/trace.zip
